name: zz-validate-reusable
# Temporary: confirms reusable-ci instantiates (no startup_failure) from a
# caller using the org default token (contents: read). Removed after validation.
on:
  push:
    branches: [fix/restore-enriched-notify]

jobs:
  test:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      runner: arc-k8s
      run_node: false
      run_docker_build: false
      run_kustomize: false
    secrets: inherit
