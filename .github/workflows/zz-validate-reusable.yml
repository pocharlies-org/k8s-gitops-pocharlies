name: zz-validate-reusable
# Temporary: confirms reusable-ci instantiates (no startup_failure). Removed after.
on:
  push:
    branches: [fix/notify-logs-via-pat]

jobs:
  test:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      runner: arc-k8s
      run_node: false
      run_docker_build: false
      run_kustomize: false
    secrets: inherit
