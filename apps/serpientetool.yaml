apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: serpientetool
  namespace: argocd
  annotations:
    link.argocd.argoproj.io/GitHub: https://github.com/pocharlies-org/k8s-serpientetool-pocharlies
  labels:
    e-dani.com/tier: app
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/pocharlies-org/k8s-serpientetool-pocharlies
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: serpientetool
  syncPolicy:
    automated:
      selfHeal: true
      # The first sync adopts resources that were applied manually. Keep
      # pruning disabled until the adoption diff and rollback have been
      # exercised in production.
      prune: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
